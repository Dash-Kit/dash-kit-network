name: example
description: Dash-Kit Network example project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  built_collection: ^5.1.0
  built_value: ^8.1.2
  built_value_generator: ^8.0.6
  cupertino_icons: ^1.0.3
  dash_kit_network:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  build_resolvers: ^2.0.4
  build_runner: ^2.1.1
  flutter_test:
    sdk: flutter
  json_serializable: ^4.1.3

flutter:
  uses-material-design: true
