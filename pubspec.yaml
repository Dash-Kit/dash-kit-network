name: dash_kit_network
description: The DashKit component that provides basic network functionality includes a token refreshing.
version: 2.0.5
homepage: https://github.com/Dash-Kit/dash-kit-network

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.24.1

  dio: ^3.0.9
  flutter_secure_storage: ^3.3.3
  shared_preferences: ^0.5.8
  meta: ^1.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  test: ^1.6.0

flutter:
