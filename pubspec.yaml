name: flutter_platform_network
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.22.3

  dio: ^3.0.3
  json_serializable: ^2.2.1
  flutter_secure_storage: ^3.3.1+1
  shared_preferences: ^0.5.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  test: ^1.6.0

flutter:
